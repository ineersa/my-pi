# catppuccin-mocha usage

Use this theme when you want a soft, low-contrast palette with pastel accents for long coding sessions.

Apply via `/settings` in pi or by setting `theme` to `catppuccin-mocha` in project/global settings.
