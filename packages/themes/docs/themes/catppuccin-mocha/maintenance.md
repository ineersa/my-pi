# catppuccin-mocha maintenance

Safe modification checklist:
- Edit only `packages/themes/themes/catppuccin-mocha.json`.
- Keep `name` as `catppuccin-mocha` unless intentionally creating a new theme id.
- Preserve valid JSON and existing key structure under `vars` and `colors`.
- Test by selecting the theme in pi after changes.
