# gruvbox-dark maintenance

Safe modification checklist:
- Edit only `packages/themes/themes/gruvbox-dark.json`.
- Keep `name` as `gruvbox-dark` unless intentionally creating a new theme id.
- Preserve valid JSON and existing key structure under `vars` and `colors`.
- Test by selecting the theme in pi after changes.
