# gruvbox-dark usage

Use this theme for warm, earthy colors with balanced contrast and a classic terminal vibe.

Apply via `/settings` in pi or by setting `theme` to `gruvbox-dark` in project/global settings.
