# oh-p-dark usage

Use this theme for a clean dark UI with teal/blue accents inspired by the original oh-pi style.

Apply via `/settings` in pi or by setting `theme` to `oh-p-dark` in project/global settings.
