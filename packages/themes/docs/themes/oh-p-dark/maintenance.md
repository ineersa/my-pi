# oh-p-dark maintenance

Safe modification checklist:
- Edit only `packages/themes/themes/oh-p-dark.json`.
- Keep `name` as `oh-p-dark` unless intentionally creating a new theme id.
- Preserve valid JSON and existing key structure under `vars` and `colors`.
- Test by selecting the theme in pi after changes.
