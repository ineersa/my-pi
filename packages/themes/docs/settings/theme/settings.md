# Theme setting

Theme selection is controlled by the `theme` field in pi settings.

Supported scope:
- Project: `.pi/settings.json`
- Global: `~/.pi/agent/settings.json`

Example:

```json
{
  "theme": "tokyo-night"
}
```
