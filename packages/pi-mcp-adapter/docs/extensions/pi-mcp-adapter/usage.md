# pi-mcp-adapter usage

Provides:

- `/mcp` command (`status`, `tools`, `reconnect`)
- `mcp` gateway tool (status/list/search/describe/connect/call)
- optional direct MCP tool registration

This local fork is intentionally slim (no OAuth/UI panel flow).
