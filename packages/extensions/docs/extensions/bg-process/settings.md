# bg-process settings

This extension has no user config file.

Runtime controls:

- `bash.timeout` tool parameter (seconds) controls when the background prompt appears.
- Default threshold is 15 seconds.
