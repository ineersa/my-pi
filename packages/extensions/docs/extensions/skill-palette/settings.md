# skill-palette settings

Optional theme file locations:

- `extensions/skill-palette/theme.json` (preferred)
- `~/.pi/agent/extensions/pi-skill-palette/theme.json` (legacy fallback)

If no theme file exists, built-in defaults are used.
