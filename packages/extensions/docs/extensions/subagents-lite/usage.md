# subagents-lite usage

Commands:

- `/run-agent <name> -- <task>`
- `/subagents-status`
- `Ctrl+Alt+S` shortcut opens status overlay

Tool:

- `launch_subagents` (max 4 agents, duplicates allowed)

Runs execute in tmux panes and report back to the parent session.
