# custom-footer settings

This extension has no dedicated user config file.

It reacts to runtime/session state (model, usage, git branch, extension statuses).

Optional external dependency:

- `gh` CLI is used opportunistically to probe PR metadata.
