# usage usage

Command:

- `/usage`

What it reports:

- OpenAI Codex usage/quota windows (when auth is available)
- z.ai quota windows and model visibility (when configured)
- current session token/cost totals

If provider auth is missing or expired, output includes remediation hints.
