# usage settings

This extension has no dedicated settings file.

Runtime inputs it can use:

- `~/.pi/agent/auth.json` provider entries (OpenAI Codex, optional `zai`)
- optional env var `ZAI_API_KEY`
