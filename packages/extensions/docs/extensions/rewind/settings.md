# rewind settings

Configured in `~/.pi/agent/settings.json` under `rewind`:

- `silentCheckpoints`
- `retention.maxSnapshots`
- `retention.maxAgeDays`
- `retention.pinLabeledEntries`
- `retention.scanMode` (`ancestor-only` or `repo-sessions`)
- `retention.startupBudgetMs`
