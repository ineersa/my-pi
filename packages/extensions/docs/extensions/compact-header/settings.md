# compact-header settings

Supported settings/flags:

- CLI flag: `--plain-icons`
- Settings key: `plainIcons` in `.pi/settings.json` (project) or `~/.pi/agent/settings.json` (global)
- Env bridge: `MY_PI_PLAIN_ICONS` and `OH_PI_PLAIN_ICONS`

No other user settings are required.
