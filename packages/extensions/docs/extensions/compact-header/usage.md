# compact-header usage

Shows a sticky header widget above the editor with:

- prompt and skill command hints
- subagent availability/running snapshot
- MCP server status summary (when available)

Widget is set on `session_start` and refreshed on `turn_end`.
