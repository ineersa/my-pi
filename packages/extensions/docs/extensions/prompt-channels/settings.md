# prompt-channels settings

This extension has no user-configurable settings.

It operates automatically whenever loaded, relocating project context files and
skills registry from the system prompt to user-level custom messages.
