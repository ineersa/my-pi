# session-status usage

Adds lightweight session indicators:

- sets footer status key `my-pi` on `session_start`
- clears it on `session_shutdown`
- command `/my-pi` reports bundle health and extension command count
