# pi-settings

Repo snapshot of global pi settings from `~/.pi` for bootstrap.

- Pull from your machine: `npm run copy:settings -- pull`
- Push to your machine: `npm run copy:settings -- push`

`auth.json` is always excluded (see `.gitignore`).
