# intercom settings

Config file: `~/.pi/agent/intercom/config.json`

Keys:

- `enabled` (default `true`)
- `confirmSend` (default `false`)
- `replyHint` (default `true`)
- `status` (optional advertised status)
